services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - paddle_models:/models/paddle
      - ocr_debug:/data/ocr-debug

volumes:
  paddle_models:
  ocr_debug:
